#include <stdio.h>

int main(int argc, char **argv)
{
    int i, j;
    float x, y;
    scanf("%d%d%f%f", &i, &j, &x, &y);

    printf("%d, %d\n", i, j);
    printf("%f, %f\n", x, y);

    return 0;
}
